class Solution {
  /**
   * @param {number[]} arr
   * @param {number} x
   * @return {boolean}
   */
  findElement(arr, x) {
    // Your code goes here
    return arr.indexOf(x) === -1 ? false : true;
  }
}
